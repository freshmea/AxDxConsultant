# codex cli 프롬프트

```text

codex cli 를 쓸려고 한다. 설치를 전역으로 진행해 주고 path도 등록해서 터미널을 열었을 때 바로 실행 될 수 있게 설정해줘.

opencode 를 설치를 해서 codex cli 와 비교를 하려고 한다. 설치를 진행해줘

opencode 를 port 를 열어서 브라우저로 접근할 수 있고 api 로 접근할 수 있게 실행해줘

```
