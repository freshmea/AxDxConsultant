# crewai 설치 프롬프트

```text

crewai 를 설치하고 실행을 하려고 한다. 계획을 세워줘 llm은 local llm을 써서 구동을 해줘
http://182.229.102.180:30003/ 여기에 llama.cpp 로 gemma4 모델을 구동 중이다.

yaml 형식으로 agent와 task를 불러 올 수 있는 구조를 만들어서 범용적으로 쓸 수 있게 구조를 만들어줘

```

- firecrawl 테스트 명령어
- firecrawl 에 회원가입 해야 한다.
- .env 파일에 api 키를 넣어야 한다. FIRECRAWL_API_KEY=발급받은 api 키
- 파일을 만들어야 한다. test_firecrawl.py

```bash
uv add crewai-tools firecrawl-py
uv run .\src\test_firecrawl.py "최수길 github"
```
